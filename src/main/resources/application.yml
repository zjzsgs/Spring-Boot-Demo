spring:
  profiles:
    #使用 dev 或者 product 的配置
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/sb
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true